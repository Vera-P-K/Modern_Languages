#ifndef STROKA_H    // Если символ STROKA_H еще не определен...
#define STROKA_H    // ...определить его (защита от повторного включения файла)

#include <iostream> // Подключаем ввод-вывод (cin, cout)
#include <cstring>  // Подключаем функции для C-строк (strcpy, strlen)

class Stroka {      // Начало описания класса
    char str[80];   // Поле данных: массив символов фиксированного размера (80 байт)

public:             // Раздел доступных (публичных) методов
    Stroka(const char*);       // Прототип конструктора для создания из обычной строки
    Stroka();                  // Прототип конструктора по умолчанию (пустая строка)
    Stroka(const Stroka&);     // Прототип конструктора копирования
    Stroka& operator=(const Stroka&); // Прототип оператора присваивания
    Stroka& operator+(const Stroka&); // Прототип оператора сцепления (сложения)
    int operator==(const Stroka&);    // Прототип оператора сравнения
    int dlina();                      // Метод для получения длины строки
    void vvod();                      // Метод для ввода строки с клавиатуры
    void vyvod();                     // Метод для вывода строки на экран
};

#endif              // Конец блока защиты #ifndef

